spring:
  application:
    name: ccshareapp
  datasource:
    url: jdbc:h2:mem:cardsharer
    driverClassName: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop