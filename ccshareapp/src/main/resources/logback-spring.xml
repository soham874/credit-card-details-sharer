<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}/}spring.log}"/>
    <property name="LOG_FILE_MAX_SIZE" value="10MB"/>
    <property name="LOG_FILE_MAX_HISTORY" value="30"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>${LOG_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${LOG_FILE_MAX_HISTORY}</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Application Loggers -->
    <logger name="com.soham.ccshareapp" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <!-- Spring Framework Loggers -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.security" level="DEBUG"/>
    <!--
        RequestResponseBodyMethodProcessor at DEBUG prints the whole deserialized
        request body. That means `card_identifier` and `encrypted_cc_blob` in the
        clear on every /create — both of which LLD §6.7 forbids logging, and the
        identifier is now derived from the passkey, so a log line carrying one is
        an offline oracle for grinding it. Application code routes identifiers
        through `LogSafe`; this logger bypassed that entirely. Keep it at INFO.
    -->
    <logger name="org.springframework.web.servlet.mvc.method.annotation.RequestResponseBodyMethodProcessor" level="INFO"/>

    <!-- Hibernate/JPA Loggers -->
    <!--
        Statement text only. `BasicBinder` at TRACE was removed deliberately: it
        logs every bound parameter, which on this schema is the identifier, the
        SRP verifier, the salt, and the ciphertext (LLD §6.7 again).
    -->
    <logger name="org.hibernate.SQL" level="DEBUG"/>

    <!-- Third-party Libraries -->
    <logger name="com.zaxxer.hikari" level="INFO"/>
    <logger name="org.springframework.boot.actuate" level="INFO"/>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Spring Profiles -->
    <springProfile name="dev">
        <logger name="com.soham.ccshareapp" level="DEBUG"/>
    </springProfile>

    <springProfile name="prod">
        <logger name="com.soham.ccshareapp" level="INFO"/>
        <root level="WARN">
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>
</configuration>
